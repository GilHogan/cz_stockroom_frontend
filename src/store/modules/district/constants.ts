export enum GetterConstants {
  GET_DISTRICT_ITEMS = "DISTRICT/GET_ITEMS",
  GET_DISTRICT_CHART_SOURCE = "DISTRICT/GET_CHART_SOURCE",
}

export enum MutationConstants {
  HANDLE_DISTRICT_ITEMS = "DISTRICT/HANDLE_ITEMS",
  UPDATE_STATUS = "DISTRICT/UPDATE_STATUS",
}

export enum ActionConstants {
  DISTRICT_FETCH = "DISTRICT/FETCH",
}

export enum DistrictType {
  TianNing = 0, //  天宁区
  ZhongLou = 1, //  钟楼区
  XinBei = 2, //  新北区
  WuJin = 3, //  武进区
}
